// /dashboard — redirects to the full modular dashboard
'use client';
import { SafariChargeDashboardApp } from '@/app/page-dashboard';
export default function DashboardPage() {
  return <SafariChargeDashboardApp />;
}
